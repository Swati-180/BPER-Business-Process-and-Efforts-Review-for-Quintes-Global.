{
  "name": "qg-tools-bper",
  "version": "1.0.0",
  "description": "Root package for running QG Tools BPER Platform",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm start",
    "client": "cd bper-frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../bper-frontend && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
